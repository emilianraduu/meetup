import {View} from 'react-native';
import React from 'react';
import {theme} from '../../helpers/constants';

const HistoryCard = ({navigation}) => {
  return <View style={{flex: 1, backgroundColor: theme.white}}></View>;
};

export default HistoryCard;
